# Copy this file to .env and fill in the values

# Azure OpenAI Configuration
# Fill in your actual values

# Server-side environment variables (secure, not exposed to browser)
# Required: Your Azure OpenAI API key
# AZURE_OPENAI_API_KEY=your_azure_openai_api_key_here

# # Required: Your Azure OpenAI endpoint URL
# AZURE_OPENAI_ENDPOINT=https://your-resource-name.cognitiveservices.azure.com/

# # Optional: Model name (defaults to gpt-4o if not specified)
# AZURE_OPENAI_MODEL_NAME=gpt-4o

# # Optional: Deployment name (defaults to gpt-4o if not specified)
# AZURE_OPENAI_DEPLOYMENT=gpt-4o

# # Optional: API version (defaults to 2024-08-01-preview if not specified)
# AZURE_OPENAI_API_VERSION=2024-08-01-preview

# Client-side environment variables (exposed to browser - use with caution!)
# Only use these if you need to call the API from client-side code
# Note: Exposing API keys on client-side is a security risk!
NEXT_PUBLIC_AZURE_OPENAI_API_KEY=your_azure_openai_api_key_here
NEXT_PUBLIC_AZURE_OPENAI_ENDPOINT=https://your-resource-name.cognitiveservices.azure.com/
NEXT_PUBLIC_AZURE_OPENAI_MODEL_NAME=gpt-4o
NEXT_PUBLIC_AZURE_OPENAI_DEPLOYMENT=gpt-4o
NEXT_PUBLIC_AZURE_OPENAI_API_VERSION=2024-08-01-preview